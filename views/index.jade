extends layout

block content
    div(ng-app='myApp', ng-controller='customersCtrl')
      ul
        li(ng-repeat='x in myData')
          | {{ x.userId + ', ' + x.id + x.title}}
    script(src='/javascripts/listing_angular.js')
     
      